export interface Occasion {
    id: number;
    name: string;
}

export interface Gender {
    id: number;
    name: string;
}

export interface Price {
    id: number;
    name: string;
}

export interface Season {
    id: number;
    name: string;
}